[pytest]
DJANGO_SETTINGS_MODULE = care_plan.settings_test
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (mock external deps, test logic only)
    integration: Integration tests (Django test client, full HTTP cycle)
